# Vision — Call Analytics

> Создано Vision Team: 2026-02-25
> Статус: **CONDITIONAL GO** → 4 блокера разрешены в ARCHITECTURE.md

---

## Запрос пользователя

> Разработать веб-приложение для аналитики звонков операторов. Функционал: загрузка аудиофайлов, транскрибация (Whisper), анализ по критериям через LLM (Стандарты, Лояльность, Доброжелательность) с выводом результатов в процентах и общем резюме.

## Интерпретация

Пользователь хочет **инструмент контроля качества работы операторов контакт-центра**. Это не просто «загрузить аудио и получить текст» — это полноценная **QA-система с автоматическим скорингом**.

**Ключевой инсайт:** система автоматизирует рутину супервайзера — вместо ручного прослушивания каждого звонка, он загружает файлы и получает структурированные оценки по единым критериям.

Три критерия оценки:
- **Стандарты** — соблюдение скрипта/протокола: приветствие, представление, уточнение вопроса, прощание, предложение доп. услуг
- **Лояльность** — клиентоориентированность: стремление решить проблему, удержание клиента, работа с возражениями
- **Доброжелательность** — тон общения: вежливость, эмпатия, отсутствие грубости/раздражения, спокойствие в конфликтных ситуациях

---

## Видение продукта

Веб-приложение (SPA), в которое супервайзер перетаскивает аудиофайлы звонков и через несколько минут получает:
- Полный транскрипт с разделением реплик оператора и клиента (диаризация)
- Автоматические оценки оператора по трём критериям (0-100%)
- Текстовое резюме с цитатами из разговора
- Историю всех проанализированных звонков с фильтрацией и поиском

Pipeline обработки: **Upload → Whisper (транскрибация) → pyannote (диаризация) → GPT-4 (анализ) → Результаты в БД**.

---

## Целевая аудитория

### Основные пользователи
1. **Супервайзер / Руководитель группы** — главный пользователь. Загружает звонки, просматривает оценки, формирует обратную связь
2. **QA-менеджер** — отвечает за стандарты обслуживания, анализирует тренды

### Вторичные пользователи
3. **Руководитель контакт-центра** — смотрит агрегированную статистику

### Не пользователи (MVP)
- Клиенты (внутренний инструмент)
- Операторы (доступ к своим оценкам — v2.0+)

---

## Ключевые сценарии

### Основные

**1. Супервайзер проверяет звонки утренней смены**
1. Открывает приложение → видит зону drag-and-drop + историю прошлых анализов
2. Перетаскивает 8 файлов → указывает оператора (выбор из списка или ввод)
3. Нажимает «Анализировать» → видит прогресс по этапам для каждого файла
4. Первые результаты появляются через ~30 сек — уже можно смотреть
5. Замечает звонок с 58% → кликает → видит резюме + подсвеченные проблемные цитаты
6. Слушает проблемный фрагмент в аудиоплеере (клик по timestamp)

**2. QA-менеджер ищет проблемные звонки**
1. Открывает историю → фильтрует: оператор «Иван», оценка < 70%, за прошлую неделю
2. Видит 3 проблемных звонка → открывает каждый, читает резюме
3. Формирует обратную связь для планёрки

**3. Быстрая проверка одного звонка**
1. Перетаскивает 1 файл → «Анализировать»
2. ~20 сек → результат автоматически раскрывается (для одного файла)
3. Оценки 87%, 92%, 95% → «Всё ок», закрывает

### Edge Cases

| Сценарий | Поведение системы |
|----------|-------------------|
| Диаризация ошибилась (оператор↔клиент) | Показать confidence score диаризации. При < 70% — предупреждение «Разделение говорящих неуверенное, проверьте вручную» |
| Длинный звонок (60+ мин) | Chunked-обработка Whisper. Прогресс по сегментам. Результаты собираются в единый транскрипт |
| Несколько операторов в одном звонке (перевод) | pyannote обнаружит > 2 спикера → предупреждение «Обнаружено более 2 говорящих. Оценка может быть неточной» |
| Моно-запись (без каналов) | pyannote speaker diarization (по голосу). Speaker_0 = оператор (первый голос), speaker_1 = клиент |
| Стерео-запись (каналы) | Разделение каналов: левый = оператор, правый = клиент. Быстрее и точнее pyannote |
| Музыка / IVR / тишина | Фильтрация non-speech сегментов перед анализом (VAD). Whisper пометит как [music], [silence] |
| Пустой / тихий файл | «Не удалось распознать речь. Проверьте что файл содержит голосовую запись» |
| Плохое качество звука | Обработка идёт, в резюме: «Низкое качество записи может влиять на точность» |
| Дубликат файла (SHA-256) | Диалог: «Этот файл уже обработан. Показать результат / Обработать заново?» |
| LLM content policy block | «Автоматический анализ недоступен для этого звонка. Транскрипт доступен для ручной проверки» |

---

## Пользовательский опыт

### UX-принципы
1. **Прозрачность** — каждый этап виден: загрузка → транскрибация → диаризация → анализ → готово
2. **3 клика до результата** — открыл → загрузил → получил оценку
3. **Информационная иерархия** — общая оценка крупно, детали по запросу
4. **Пакетная работа как норма** — 1 файл и 20 файлов обрабатываются одинаково удобно
5. **Streaming results** — готовые файлы показываются сразу, не ждут остальных

### SPA с состояниями
```
Empty (drag-drop зона + история) → Upload (список файлов + оператор) →
Processing (прогресс по этапам) → Results (таблица + сводка)
```

### Результаты — визуальная иерархия
```
┌──────────────────────────────────────────────────────┐
│  Logo   Call Analytics              [+ Загрузить]    │
├──────────────────────────────────────────────────────┤
│  ┌────────────┐  ┌────────────┐  ┌────────────┐     │
│  │ Стандарты  │  │ Лояльность │  │ Доброжел.  │     │  ← Сводные карточки
│  │    85%     │  │    79%     │  │    91%     │     │
│  └────────────┘  └────────────┘  └────────────┘     │
│                                                      │
│  [Фильтры: оператор ▾  дата ▾  оценка ▾  поиск 🔍] │
│                                                      │
│  ┌─ Результаты ────────────────────────────────┐    │
│  │ Файл         │Оператор│Станд.│Лоял.│Добр.│Итог│ │
│  │ call_001.mp3  │ Иван  │ 87%  │ 92% │ 95% │91% │ │
│  │ ▸ [expandable row → транскрипт, цитаты]     │    │
│  │ call_002.mp3  │ Мария │ 74%  │ 81% │ 88% │81% │ │
│  └──────────────────────────────────────────────┘   │
│  [◄ 1 2 3 ... ►]  20 из 156 результатов            │  ← Пагинация
└──────────────────────────────────────────────────────┘
```

### Детальный отчёт
- Общая оценка (крупно, цветовая индикация)
- Оценки по критериям: 0-100% + текстовый лейбл (Отлично / Хорошо / Удовлетворительно / Требует внимания)
- Резюме AI с цитатами из разговора
- Транскрипт с подсветкой проблемных мест и разделением оператор/клиент
- Аудиоплеер с навигацией по timestamp (клик → воспроизведение с этого момента)

### Цветовая система оценок
| Диапазон | Цвет | Лейбл | Hex |
|----------|------|-------|-----|
| 90-100% | Зелёный | Отлично | `#22C55E` |
| 75-89% | Синий | Хорошо | `#3B82F6` |
| 60-74% | Жёлтый | Удовлетворительно | `#F59E0B` |
| 0-59% | Красный | Требует внимания | `#EF4444` |

Цвет **не единственный** индикатор — всегда дублируется текстовым лейблом (accessibility).

---

## Отказоустойчивость

### Pipeline с checkpoints
```
Upload → [save to disk] → Transcribe (Whisper) → [save transcript] →
Diarize (pyannote) → [save diarization] → Analyze (GPT-4) → [save analysis] → Done
```
Каждый checkpoint сохраняется в PostgreSQL. При падении на этапе N — продолжение с N, не с начала.

### 4 уровня Graceful Degradation
| Уровень | Условие | Что доступно |
|---------|---------|-------------|
| **1. Полный** | Всё работает | Транскрипт + диаризация + оценки + резюме |
| **2. Без анализа** | GPT-4 недоступен | Транскрипт + диаризация. Кнопка «Запустить анализ позже» |
| **3. Без диаризации** | pyannote упал | Транскрипт без разделения. Предупреждение + ручная проверка |
| **4. Оффлайн** | Всё упало | Показать ранее обработанные результаты. Новые файлы — в очередь |

### Retry-стратегия
- Exponential backoff: 2с → 4с → 8с (3 попытки)
- Whisper fallback: локальный GPU → OpenAI Whisper API
- LLM: GPT-4 → graceful degradation (транскрипт без оценок)

---

## Данные и конфиденциальность

**⚠️ ВАЖНО:** GPT-4 API = отправка транскриптов в облако OpenAI.

| Данные | Где хранятся |
|--------|-------------|
| Аудиофайлы | **Локально** на сервере (удаляются через 7 дней, конфигурируемо) |
| Транскрипты | **Локально** в PostgreSQL + **отправляются** в OpenAI API для анализа |
| Результаты анализа (оценки, резюме) | **Локально** в PostgreSQL |
| API-ключ | **Локально** в .env файле |

**Рекомендация для полной конфиденциальности:** замена GPT-4 на локальный LLM (llama3, mistral) возможна, но потребует значительно больше GPU-ресурсов и может снизить качество анализа.

---

## Границы scope

### MVP (v1.0)
- Загрузка одного или пачки аудиофайлов (drag-and-drop)
- Привязка файлов к оператору (ввод имени или выбор из списка)
- Транскрибация (Whisper large-v3, локально на GPU)
- Диаризация (pyannote: стерео — split каналов, моно — speaker diarization)
- Анализ по 3 критериям через GPT-4 (Стандарты, Лояльность, Доброжелательность)
- Результаты: оценки 0-100% + текстовое резюме + цитаты
- Аудиоплеер с навигацией по timestamp
- История всех анализов в PostgreSQL
- Пагинация, фильтрация (оператор, дата, оценка), поиск
- Graceful degradation (4 уровня)

### Будущее (v1.1)
- Экспорт отчётов (PDF, CSV)
- Агрегация по оператору (средние оценки за период)
- Комментарии / пометки к звонкам
- Уведомления о критичных оценках (< 40%)
- Настраиваемые критерии оценки (добавление/удаление)

### Будущее (v2.0+)
- Трендовая аналитика и сравнение операторов
- Ролевая модель (авторизация)
- Интеграция с CRM/HR
- Кастомные весовые коэффициенты критериев
- Пакетная переоценка при изменении критериев
- Локальный LLM как альтернатива GPT-4

---

## Открытые вопросы (для уточнения по ходу)

1. **Формат имени оператора** — свободный текст или справочник? MVP: свободный текст + автодополнение из ранее введённых имён
2. **Длинные звонки (30+ мин)** — разбивать Whisper на чанки (30 сек с overlap) и собирать обратно
3. **Фильтрация музыки/IVR** — использовать VAD (Voice Activity Detection) перед Whisper
4. **Стабильность LLM-оценок** — temperature=0 для детерминизма, seed если поддерживается
